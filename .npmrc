# 네트워크 타임아웃/재시도 (ETIMEDOUT 대응)
fetch-timeout=120000
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# 동시 다운로드 수 제한 (불안정한 네트워크 시 유리)
network-concurrency=8

# React Native requires flat node_modules access for Gradle/Xcode
node-linker=hoisted
