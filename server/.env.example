# ============================================
# Golden Race Server 환경 변수
# ============================================
# .env.example을 .env로 복사 후 값 설정

# PostgreSQL 연결 문자열 (Prisma)
# 로컬/클라우드 PostgreSQL Connection string
DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# 서버 포트
PORT=3001

# Google OAuth (구글 로그인)
# Google Cloud Console에서 Web Client ID 발급
GOOGLE_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com

# Google Gemini API (AI 예측)
# https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key

# JWT 시크릿 (토큰 서명용)
# openssl rand -base64 32 로 생성 가능
JWT_SECRET=your-jwt-secret-at-least-32-chars

# Redis (캐시 — 선택, 미설정 시 인메모리 캐시 사용)
# REDIS_URL=redis://localhost:6379

# Expo Push (Admin 푸시 알림 발송)
# https://expo.dev/accounts/[account]/settings/access-tokens
# EXPO_ACCESS_TOKEN=optional-for-expo-push-api

# KRA 공공데이터 API (경마 출전표, 결과, 기수전적 등)
# https://www.data.go.kr/ 에서 "한국마사회" API 활용신청 후 발급
# ※ "인코딩" 키를 그대로 입력 (공공데이터포털에서 복사한 값)
# KRA_SERVICE_KEY=인코딩된키값
