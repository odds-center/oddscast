# Golden Race - 경마 예측 앱

## 프로젝트 개요

Golden Race는 경마 애호가들을 위한 모바일 애플리케이션으로, 실시간 경주 정보, 베팅 시스템, 그리고 사용자 맞춤형 서비스를 제공합니다.

**백엔드**: NestJS + MySQL (인증, 실시간 데이터베이스)
**프론트엔드**: React Native + Expo
**인증**: Google OAuth 2.0

## 주요 기능

### 1. 사용자 인증 및 프로필

- **Google 소셜 로그인**: Google 계정 기반의 사용자 인증.
- **프로필 관리**: 사용자 정보 및 설정 관리.
- **개인화**: 사용자 선호도에 따른 맞춤형 서비스.

### 2. 경주 정보

- **경주 목록 조회**: 데이터베이스에서 가져온 예정된 경주 리스트를 날짜와 장소별로 확인.
- **경주 상세 정보**: 각 경주의 상세 정보, 출마 말 정보, 배당률 등.
- **실시간 업데이트**: 경주 상태 및 결과의 실시간 업데이트.

### 3. 베팅 시스템

- **베팅 플레이스**: 사용자가 원하는 말에 베팅할 수 있는 시스템.
- **베팅 내역**: 사용자의 베팅 기록 및 결과 확인.
- **배당률 계산**: 실시간 배당률 계산 및 표시.

### 4. 결과 및 통계

- **경주 결과**: 완료된 경주의 결과 및 순위.
- **통계 분석**: 말, 기수, 조교사별 성적 통계.
- **히스토리**: 과거 경주 결과 및 베팅 내역.

## 기술적 특징

### 1. 아키텍처

- **모듈화된 구조**: 기능별로 분리된 모듈 구조.
- **RESTful API**: 표준화된 API 설계.
- **실시간 데이터**: WebSocket을 통한 실시간 데이터 전송.

### 2. 데이터베이스 설계

- **정규화**: 효율적인 데이터 저장을 위한 정규화된 테이블 구조.
- **인덱싱**: 빠른 검색을 위한 전략적 인덱스 설계.
- **백업 및 복구**: 정기적인 데이터 백업 및 복구 시스템.

### 3. 보안

- **JWT 인증**: 안전한 사용자 인증 시스템.
- **데이터 암호화**: 민감한 데이터의 암호화 저장.
- **권한 관리**: 사용자별 접근 권한 제어.

## 데이터 모델

### 1. 핵심 엔티티

#### User (사용자)

- **기본 정보**: ID, 이메일, 이름, 프로필 이미지.
- **인증 정보**: Google ID, 로그인 상태.
- **설정**: 알림 설정, 테마 설정 등.

#### Race (경주 정보)

- **경주 기본 정보**: 경주 번호, 날짜, 시간, 장소.
- **경주 조건**: 거리, 경주로 타입, 나이 제한 등.
- **상태**: 예정, 진행중, 완료 등.

#### Horse (말)

- **기본 정보**: 이름, 나이, 성별, 혈통.
- **성적 정보**: 경주 성적, 승률, 상금 등.
- **현재 상태**: 체중, 건강 상태 등.

#### Bet (베팅)

- **베팅 정보**: 베팅 금액, 베팅 유형, 선택한 말.
- **결과**: 승패, 수익, 배당금 등.
- **상태**: 진행중, 완료, 취소 등.

### 2. 테이블 관계

- **User ↔ Bet**: 1:N (사용자당 여러 베팅)
- **Race ↔ Bet**: 1:N (경주당 여러 베팅)
- **Race ↔ Horse**: M:N (경주에 여러 말 출전)
- **Horse ↔ Bet**: 1:N (말당 여러 베팅)

## API 엔드포인트

### 1. 인증 관련

```
POST /api/auth/google/login    # Google 로그인
POST /api/auth/logout          # 로그아웃
POST /api/auth/refresh         # 토큰 갱신
```

### 2. 사용자 관련

```
GET  /api/users/profile        # 프로필 조회
PUT  /api/users/profile        # 프로필 수정
GET  /api/users/bets           # 베팅 내역
```

### 3. 경주 관련

```
GET  /api/races               # 경주 목록
GET  /api/races/:id           # 경주 상세
GET  /api/races/:id/results   # 경주 결과
```

### 4. 베팅 관련

```
POST /api/bets                # 베팅 생성
GET  /api/bets                # 베팅 목록
PUT  /api/bets/:id            # 베팅 수정
```

## 개발 환경 설정

### 1. 필수 요구사항

- Node.js 18+
- React Native 개발 환경
- MySQL 8.0+
- Google Cloud Platform 계정

### 2. 환경 변수 설정

```env
# 데이터베이스
DATABASE_URL=mysql://username:password@host:port/database

# Google OAuth
GOOGLE_CLIENT_ID=your_client_id
GOOGLE_CLIENT_SECRET=your_client_secret

# JWT
JWT_SECRET=your_jwt_secret

# 한국마사회 API
KRA_API_KEY=your_kra_api_key
```

### 3. 설치 및 실행

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev

# 앱 빌드
npm run build
```

## 배포 및 운영

### 1. 배포 환경

- **개발**: 로컬 개발 환경
- **스테이징**: 테스트 환경
- **프로덕션**: 운영 환경

### 2. 모니터링

- **로그 관리**: 구조화된 로깅 시스템
- **성능 모니터링**: API 응답 시간, 데이터베이스 성능 등
- **오류 추적**: 실시간 오류 감지 및 알림

### 3. 백업 및 복구

- **데이터 백업**: 정기적인 데이터베이스 백업
- **재해 복구**: 장애 상황 대응 계획

## 향후 계획

### 1. 기능 확장

- **AI 예측**: 머신러닝을 활용한 경주 결과 예측
- **소셜 기능**: 사용자 간 소통 및 정보 공유
- **모바일 푸시**: 실시간 알림 및 업데이트

### 2. 성능 개선

- **캐싱 시스템**: Redis를 활용한 데이터 캐싱
- **CDN**: 정적 리소스의 전역 배포
- **로드 밸런싱**: 트래픽 분산 처리

### 3. 보안 강화

- **2FA**: 이중 인증 시스템
- **감사 로그**: 사용자 활동 추적
- **보안 테스트**: 정기적인 보안 취약점 점검
