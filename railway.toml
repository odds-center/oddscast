[build]
builder = "dockerfile"
dockerfilePath = "server/Dockerfile"

[deploy]
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
# Limit memory to 512MB (down from default ~8GB unbounded)
numReplicas = 1
